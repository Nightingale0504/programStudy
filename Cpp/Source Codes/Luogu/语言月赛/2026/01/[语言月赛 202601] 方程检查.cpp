#include<bits/stdc++.h>
using namespace std;
#define int long long
const int maxN=30;
int q,n,tmp,jian,b[maxN];
string s,k[maxN];
signed main(){
    scanf("%lld",&q);
    while (q--){
        scanf("%lld",&n);
        jian=0;
        cin>>s;
        for (int i=0;i<n;i++){
            cin>>k[i];
            for (int j=0;j<k[i].size();j++){
                
            }
        }

    }
    return 0;
}