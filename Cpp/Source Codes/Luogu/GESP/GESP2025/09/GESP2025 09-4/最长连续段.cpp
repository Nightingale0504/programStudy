#include<bits/stdc++.h>
#define int long long
using namespace std;
const int maxN=15;
int n,a[maxN];
int length;
signed main(){
    scanf("%lld",&n);
    for (int i=1;i<=n;i++){
            scanf("%lld",&a[i]);
    }
    printf("%lld",length);
    return 0;
}