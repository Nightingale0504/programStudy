#include<bits/stdc++.h> 
using namespace std;
#define int long long
signed main(){
    /*
        vector
        1. define
            (1) vector<int> v; -> 定义空向量( 动态数组 )
            (2) vector<int> v2(5); -> 定义一个初始大小为 5 的动态数组
            (3) vector<int> v3(5,1); -> 定义一个初始大小为 5 , 初值为1的动态数组

        2. functions
            (1) v.push_back(x); -> 向 v 的尾部插入一个元素 x
            (2) v.insert(pos,x); -> 向 v 的 pos 位置(地址)插入元素 x(后续数据顺延)
            (3) v[i] -> 访问(数组)
            (4) v.size(); -> 返回 v 的大小(同 string/stack/queue)
            (5) v.erase(v.begin()+i); -> 删除 v 中位于 i 的元素
            (6) sort(v.begin(),v.end()); -> 排序 v
            (7) v.empty(); -> 判断是否为空(同 queue)
            (8) v.clear(); -> 清空 v 中全部元素(同 queue)
    */
	return 0;
}